% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_store.R
\name{morie_data_verify}
\alias{morie_data_verify}
\title{Verify the bundled data store against its signed manifest}
\usage{
morie_data_verify()
}
\value{
A data frame with one row per manifest entry: \code{path}, \code{bytes},
\code{sha256}, \code{ok} (the file on disk matches). The attribute
\code{"signature"} is \code{TRUE} when the manifest's signature verified, and
the function errors if it did not.
}
\description{
Every file rmoriedata ships is listed with its SHA-256 in a manifest,
and the manifest is signed with an XMSS (RFC 8391, SHA-256) key whose
public half ships with the package. Loading a table checks its file
against the manifest; this function checks all of them at once.
}
\examples{
v <- morie_data_verify()
all(v$ok)
attr(v, "signature")
}
